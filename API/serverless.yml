service: XenonBet-Simulator

provider:
  name: aws
  runtime: nodejs10.x
  region: ${opt:region, 'eu-west-1'}
  stage: ${opt:stage, 'staging'}
  # tracing: true
  # environment:
  # vpc:
  # iamRoleStatements:

functions:
  -

package:
  - individually: true

custom:
  - serverless-webpack
